# Python
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/
build/
wheels/

# Virtual envs / deps
.venv/
backend/.venv/
node_modules/
frontend/node_modules/

# Frontend build output
frontend/dist/
dist-ssr/

# FastF1 cache (1.8 GB, regenerable) + live-timing scratch files
backend/.cache/
backend/data/_smoke*.txt
backend/data/live_timing.txt
backend/data/live_*.csv
# Open-Meteo fetch cache (transient; weather.parquet is the committed artifact)
backend/data/weather_cache.json
# Polymarket de-vig fetch cache for the Benter validation (transient, network-derived)
backend/data/benter_collect.json

# Tooling / scratch
.playwright-mcp/
.DS_Store
*.local

# Claude Design handoff — reference material, not part of the app build
design_handoff_pitwall/
claude design.zip

# Secrets (none committed; backend/.env.example is the template that IS kept)
.env
backend/.env
frontend/.env

# Dev/preview screenshots — all root-level PNGs (real app assets live under */src/assets/)
/*.png

# Visual-review harness: keep the script/config/report, ignore deps + generated shots
.test-harness/node_modules/
.test-harness/shots-ai-review/
.test-harness/_review_progress.jsonl
.test-harness/package-lock.json
